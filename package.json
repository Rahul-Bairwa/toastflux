{
    "name": "my-toast-lib",
    "private": true,
    "workspaces": [
        "packages/*",
        "apps/*"
    ],
    "scripts": {
        "dev": "tsup --watch",
        "build": "tsup"
    },
    "devDependencies": {
        "tsup": "^8.5.1"
    }
}
